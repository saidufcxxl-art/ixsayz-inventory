# Что уже проверено

Дата проверки: 2026-07-01.

## Новый сценарий

- Приложение переделано под пошаговый мобильный сценарий.
- Первый экран: завозы.
- Внутри завоза создаются поставщики.
- Внутри поставщика добавляются товары.
- Форма товара упрощена: название/бренд, категория, пол, размер, закупка, продажа, количество, ценники, штрихкод.
- Артикул, телефон, комментарии и лишние поля убраны из основного рабочего потока.

## Проверено локально

- `app.js` прошел `node --check`.
- Все файлы `js/*.js` прошли `node --check`.
- Выполнен `work/verify.mjs`: `OK: core logic checks passed`.
- EAN-13 создается и проверяется.
- Расчеты поставки и прибыли работают на тестовых данных.
- `labelQty` учитывается.
- CSV UTF-8 с BOM создается.
- PNG-иконки для iPhone/Android есть в архиве.
- `manifest.json` и `sw.js` обновлены.

## Firebase

- Проект использует Firestore `users/public/...`.
- Последняя REST-проверка Firestore вернула `403 Forbidden`.
- Это означает, что в Firebase нужно опубликовать правила Cloud Firestore из `FIREBASE_SETUP.md`.

## Исправления диагностики сохранения

- Убрана сортировка `orderBy(createdAt)` из realtime-чтения, чтобы старые/неполные документы не ломали список.
- Добавлено постоянное красное сообщение, если Firestore запрещает чтение или запись.
- Повторная REST-проверка Firestore вернула пустой `403 Forbidden`, что похоже на блокировку правил или Web API key.
