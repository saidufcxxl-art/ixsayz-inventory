# Что уже проверено

Дата проверки: 2026-07-01.

## Текущий режим

- Приложение переведено в личный открытый режим без регистрации и пароля.
- Данные сохраняются в Firebase Firestore по пути `users/public/...`.
- `localStorage` и `sessionStorage` не используются для хранения базы.
- Firebase config вставлен в `firebase.js`.

## Проверено локально

- Создана требуемая структура файлов проекта.
- `app.js` прошел `node --check`.
- Все файлы `js/*.js` прошли `node --check`.
- Проверено, что Firestore-пути используют `suppliers`, `deliveries`, `products`, `categories`, `settings/main` внутри одной базы.
- Проверена функция контрольной цифры EAN-13.
- Проверена функция валидации EAN-13.
- Проверено, что `labelQty = 5` учитывается как пять этикеток в генераторе PDF.
- Проверены расчеты dashboard и поставки на тестовых данных.
- Проверен CSV UTF-8 с BOM и разделителями `;` и `,`.
- Проверено, что штрихкод в CSV отдается текстом для Excel.
- Проверены manifest и service worker для GitHub Pages.
- Проверено, что проект не требует сервера и может работать как статический сайт.
- Выполнен `work/verify.mjs`: `OK: core logic checks passed`.

## Что нужно проверить после публикации правил Firestore

- Добавление поставщика.
- Добавление поставки.
- Добавление товаров строками.
- Видимость данных на другом устройстве через тот же сайт.
- PDF ценников на телефоне.
- CSV экспорт для кассы.

Если Firestore rules еще старые и требуют `request.auth`, открытый режим не сможет записывать данные. Нужно поставить правила из `FIREBASE_SETUP.md` для `users/public/...`.

## Финальная проверка PWA-иконок и открытой базы

- Добавлены PNG-иконки для установки на телефон:
  - `assets/icons/apple-touch-icon.png`
  - `assets/icons/icon-180.png`
  - `assets/icons/icon-192.png`
  - `assets/icons/icon-512.png`
- `index.html` подключает `apple-touch-icon.png`.
- `manifest.json` подключает PNG и SVG иконки.
- `sw.js` обновлен до `ixsayz-inventory-v2` и кеширует PNG-иконки.
- Интерфейс входа/регистрации убран: приложение открывается сразу.
- Проверка реальной записи в Firestore `users/public/...` вернула `403 Forbidden`.
- Это значит, что в Firebase Console нужно опубликовать открытые правила из `FIREBASE_SETUP.md`.
